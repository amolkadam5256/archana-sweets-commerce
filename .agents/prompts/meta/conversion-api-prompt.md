# Conversion API Prompt

Design Meta CAPI events with `event_id`, `_fbp`, `_fbc`, client IP, user agent, consent state and hashed user data. Match browser events for deduplication and never send raw PII to analytics destinations.

