# GTM Generator Prompt

Generate GTM tags, triggers and variables from a documented event schema. Validate duplicate names, missing trigger references, Consent Mode V2, SPA page views and conversion deduplication before export.

