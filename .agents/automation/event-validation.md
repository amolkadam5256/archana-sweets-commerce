# Event Validation

Validation rules:

- Event name must match naming schema.
- Conversion event must include `event_id`.
- Lead events must include `form_type` or `lead_type`.
- No raw PII in GA4-bound events.

