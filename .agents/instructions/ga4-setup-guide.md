# GA4 Setup Guide

1. Use measurement ID `G-30C78ZK2G8`.
2. Keep GTM config page view disabled when app sends SPA `page_view`.
3. Register custom dimensions from `../schemas/ga4-events-schema.md`.
4. Mark lead and contact events as conversions.

