# Reusable Analytics Architecture

Use one analytics wrapper, one canonical event schema and GTM as the vendor routing layer. Server endpoints own CAPI, enhanced conversions, hashing and deduplication.

